collections:
  -
    filepattern: content/authors/*.md
    filename_regex: ^[a-z-]*\.md
    schema:
      - name:
        - type: str
  -
    filepattern: content/books/*.md
    filename_regex: ^[a-z-]*\.md
    schema:
      - name: 
        - type: str
      - author:
        - type: str
      - date_published:
        - type: str
        - required: false
